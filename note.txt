PGAN采用的是一个动态增加网络结构的方式进行高分辨率图像的生成
所以对于不同的训练集，在数据准备阶段就已经做了处理，处理如下：
1、对于一个训练数据集，会将其保存成log(width)-1个文件夹（.tfrecorders格式）---
2、不同的.tfrecorders对应不同分辨率的图像，
3、在训练阶段加载数据时，先加载低分辨率的图像集，这样符合本PGAN的构建想法



图像质量评估的具体代码在metrics中，调用代码在util_scripts中